<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">RestaurantApp</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <!-- Admin dropdown (solo visible para rol admin) -->
        <li class="nav-item dropdown" id="dropdown-admin" style="display:none">
          <a class="nav-link dropdown-toggle text-warning fw-bold" href="#" data-bs-toggle="dropdown">Admin</a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/admin/restaurants">Todos los Restaurantes</a></li>
            <li><a class="dropdown-item" href="/admin/reports">Reportes y Agregaciones</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/restaurants">Mis Restaurantes</a></li>
            <li><a class="dropdown-item" href="/restaurants/create">+ Nuevo Restaurante</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/menu">Menu</a></li>
            <li><a class="dropdown-item" href="/menu/create">+ Nuevo Item</a></li>
          </ul>
        </li>
        <!-- Owner dropdown (sin Reportes — solo para rol owner) -->
        <li class="nav-item dropdown" id="dropdown-owner" style="display:none">
          <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Owner</a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/restaurants">Mis Restaurantes</a></li>
            <li><a class="dropdown-item" href="/restaurants/create">+ Nuevo Restaurante</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/menu">Menu</a></li>
            <li><a class="dropdown-item" href="/menu/create">+ Nuevo Item</a></li>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Cliente</a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/orders">Mis Pedidos</a></li>
            <li><a class="dropdown-item" href="/orders/create">+ Nuevo Pedido</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/reviews">Mis Resenas</a></li>
            <li><a class="dropdown-item" href="/reviews/create">+ Nueva Resena</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/restaurants/nearby">📍 Restaurantes Cercanos</a></li>
          </ul>
        </li>
      </ul>
      </ul>
      <span class="navbar-text d-flex align-items-center gap-3">
        <span id="nav-user-info" class="text-white small fw-bold"></span>
        <button id="logout-btn" class="btn btn-sm btn-outline-danger">Cerrar Sesión</button>
      </span>
    </div>
  </div>
</nav>

<script>
  const userStr = localStorage.getItem('user');
  if (userStr) {
    const user = JSON.parse(userStr);
    
    // Mostrar nombre del usuario logeado 
    const userInfoEl = document.getElementById('nav-user-info');
    if (userInfoEl) userInfoEl.textContent = `Hola, ${user.name} (${user.role})`;

    // Mostrar dropdown según rol
    if (user.role === 'admin') {
      const adminDropdown = document.getElementById('dropdown-admin');
      if (adminDropdown) adminDropdown.style.display = '';
    } else if (user.role === 'owner') {
      const ownerDropdown = document.getElementById('dropdown-owner');
      if (ownerDropdown) ownerDropdown.style.display = '';
    }
    // clientes: solo ven el dropdown Cliente (siempre visible)
  }

  // Cerrar sesión
  const logoutBtn = document.getElementById('logout-btn');
  if (logoutBtn) {
    logoutBtn.addEventListener('click', () => {
      localStorage.removeItem('user');
      window.location.href = '/login';
    });
  }
</script>
